{
  "name": "work-hub",
  "version": "1.0.0",
  "description": "A local web dashboard over several project folders at once, plus a console for the Claude Code conversations that happened in them.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/serve.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "node-pty": "^1.1.0"
  },
  "devDependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0"
  },
  "allowScripts": {
    "node-pty@1.1.0": true
  }
}
